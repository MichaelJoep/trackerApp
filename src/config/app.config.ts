export const APP_CONFIG = {
    /**
     * Keep false while the backend is not ready.
     *
     * When your Express backend is ready:
     *
     * USE_BACKEND: true
     */
    USE_BACKEND: false,
  } as const;